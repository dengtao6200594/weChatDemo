.tabs_title {
  display: flex;
}
.tabs_title .tab_item {
  flex: 1;
  text-align: center;
  padding: 15rpx 0;
}
.tabs_title .active {
  color: var(--themeColor);
  border-bottom: 5rpx solid currentColor;
}
